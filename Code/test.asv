clear;
n = 10; k = 4; d = 16; e = 0.7;
A = rand(d,n);
tmp = zeros([4,n]);
Phi = (createFJLT1projectionMatrix(k,d,n,e));

for i = 1:n
    x = A(:,i);
    px = Phi*x;
    tmp(1,i) = (1-e)*k*norm(x,2);
    tmp(2,i) = norm(px,k);
    tmp(3,i) = (1+e)*k*norm(x,2);
    if (tmp(2,i) >= tmp(1,i)) && )
end
